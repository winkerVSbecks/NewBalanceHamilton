#ifndef GLOBAL_H // header guards
#define GLOBAL_H

// extern tells the compiler this variable is declared elsewhere
extern int noActiveWidgets;
extern int timerLength;

extern int slide1;
extern int slide2;
extern int slide3;

extern float momentumForAll;

#endif
